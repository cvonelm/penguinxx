@PACKAGE_INIT@

if (NOT TARGET penguinxx::core)
    find_package(fmt REQUIRED)
    find_package(PkgConfig REQUIRED)
    pkg_check_modules(NUMA REQUIRED IMPORTED_TARGET numa)
    include(${CMAKE_CURRENT_LIST_DIR}/penguinxx-targets.cmake)
endif ()

check_required_components(penguinxx)
